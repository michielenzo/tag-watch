# tag-watch application

See the [project README](../README.md) for setup, platform prerequisites, timer behavior, persistence limitations and validation details.

```sh
npm ci
npm start
```

In a second terminal, run `npm run windows`, `npm run android` or `npm run ios` with the appropriate native toolchain installed.
